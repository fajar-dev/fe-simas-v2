export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      error: 'red',
      warning: 'yellow',
      neutral: 'neutral'
    },
    button: {
      defaultVariants: {
        size: 'lg'
      }
    },
    input: {
      defaultVariants: {
        size: 'lg'
      }
    },
    selectMenu: {
      defaultVariants: {
        size: 'lg'
      }
    },
    select: {
      defaultVariants: {
        size: 'lg'
      }
    },
    textarea: {
      defaultVariants: {
        size: 'lg'
      }
    }
  }
})
